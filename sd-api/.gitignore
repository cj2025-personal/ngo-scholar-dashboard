node_modules
.env
.env.*
!.env.example

# GCP service account credentials — never commit
# (anchored so src/config/ source files stay tracked)
/config/
*.key.json
service-account*.json
gcp-*.json

# Purge run artefacts: data dumps and the log of what was deleted
.purge-backup/
purge-log.txt

# Local eval output
eval-results.json
