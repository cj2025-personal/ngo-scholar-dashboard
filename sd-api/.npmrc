# The Node version in package.json `engines` is a requirement, not a hint.
#
# Without this, npm prints a warning nobody reads and the mismatch surfaces
# later as something that looks unrelated: on Node 20 the test runner cannot
# expand the globs the test scripts use and reports "Could not find", which
# reads as a missing file rather than a wrong runtime.
engine-strict=true
